This is Suresh D Test
Work in Progress
---------------
git --version

cd G:\gittutorialseris

cls

git init
*** creates a .git , it will not sync, it is local repository file.

dir


git global config
set up user id and email

$ git config --global user.name "John Doe"
$ git config --global user.email johndoe@example.com


git add

git status
*** no commits yet, untracked files:

git add README.md

git commit -m "This is the first commit"







